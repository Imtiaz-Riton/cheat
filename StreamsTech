					Database resoter command

	pg_restore --dbname=wasa_gis --host=localhost -U postgres --verbose wasa_gis2503.tar

					Alter User
	
	ALTER USER user_name WITH PASSWORD 'new_password';
	
	url(r'',include('appname.urls'))
	rul(r'^(?<pk>[0-9]+)/go/$', view.funcname)
